#!/bin/sh

for SAMPLE in *
do
	if [ -d $SAMPLE/NODEJS ] && [ -e $SAMPLE/NODEJS/RunTest.sh ]
	then
		cd $SAMPLE/NODEJS
		echo "$SAMPLE running"
		sh RunTest.sh
		cd ../..
		echo "$SAMPLE finished. Press enter to continue..."
		read -p "$*" a
	fi
done
